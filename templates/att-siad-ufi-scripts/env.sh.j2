#!/bin/bash
#Environment Variables

export HTTP_HOST="{{ siad_resource_host }}"
export RSYSLOG_HOST="{{ siad_resource_host }}"
export SIAD_URL="{{ siad_resource_url }}"
export UFI_DIAG_VERSION="{{ siad_ufi_diag_version }}"
export BSP_VERSION="{{ siad_bsp_version }}"
export BSP_RELEASE="{{ siad_bsp_release }}"
export BIOS_PREFIX="{{ siad_bios_prefix }}"
export BIOS_VERSION="{{ siad_bios_version }}"
export BMC_VERSION="{{ siad_bmc_version }}"
export CPU_CPLD_VERSION="{{ siad_cpu_cpld_version }}"
export CPU_CPLD_RELEASE="{{ siad_cpu_cpld_release }}"
export MB_CPLD_VERSION="{{ siad_mb_cpld_version }}"
export MB_CPLD_RELEASE="{{ siad_mb_cpld_release }}"
export TENGEKR_VERSION="{{ siad_tengekr_version }}"
export I210_VERSION="{{ siad_i210_version }}"
export I210_RELEASE="{{ siad_i210_release }}"
export UCD_VERSION="{{ siad_ucd_version }}"
export UCD_RELEASE="{{ siad_ucd_release }}"
export TPM_VERSION="{{ siad_tpm_version }}"
export ONIE_VERSION="{{ siad_onie_version }}"
export VYATTA_VERSION="{{ siad_vyatta_version }}"
export CURRENT_BIOS_VERSION="{{ siad_bios_prefix }}{{ siad_bios_version }}"
export CURRENT_ME_STRING="{{ current_me_version }}"
